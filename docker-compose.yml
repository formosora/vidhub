services:
  vidhub:
    build: .
    container_name: vidhub
    ports:
      - "8081:8080"
    environment:
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:-change-me}
    volumes:
      - vidhub-data:/app/data
    restart: unless-stopped

volumes:
  vidhub-data:
